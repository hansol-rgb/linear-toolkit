---
title: 변경 내역
description: linear-toolkit 슬랙 봇의 기능 업데이트 및 버그 픽스 이력
lastUpdated: 2026-04-27
---

# 변경 내역

`linear-toolkit` 프로젝트(Slack ↔ Linear 자동화 봇)의 개발 진행 동안 추가된 기능과 수정된 버그 이력을 정리한 문서.

기간: **2026-04-10 ~ 2026-04-21**, Vercel 운영 중.

---

## 기능 업데이트

### 2026-04-10 — 프로젝트 시작

- **데일리 스크럼 봇 MVP** (`1427ea0`)
  - 매일 오전 7시 KST에 팀원에게 Slack DM 발송
  - AI 인터뷰 후 대화 종료 시 Linear 이슈 자동 생성
  - 채널 스레드에 결과 포스팅까지 한 사이클 구축

### 2026-04-13 — 이모지 이슈 + 봇 자산

- **이모지 반응으로 Linear 이슈 자동 생성** (`9599bcc`) — M4 마일스톤
  - `:task:` / `:emoji-task:` / `:bug:` / `:zap:` 등으로 즉석 이슈화
- **봇 아바타 및 task 이모지 PNG 생성** (`748f7f2`)
  - `scripts/generate-emoji.mjs`로 SVG → PNG 변환

### 2026-04-14 — 품질 기반 다지기

- **템플릿 연결 + 중복 감지 + 메타데이터 자동 설정** (`2dd02fd`)
  - 팀별 템플릿 적용, AI가 `isExistingIssue` 플래그로 중복 케이스 감지
- **AI가 PRD/PROJ 팀 자동 판단하여 이슈 배정** (`cc679a2`)
  - 채널 맥락 기반으로 팀 라우팅

### 2026-04-15 — 매칭 정교화

- **프롬프트 md 분리 + 채널명 기반 팀 판단 + 커맨드 확장** (`09ad9ab`) — M1 마일스톤
  - `src/prompts/*.md`로 프롬프트 분리해 재배포 없이 조정 가능
- **프로젝트 자동 매칭 3단계** (`4dd8160`)
  - 채널명 → AI → DM 확인 순서로 fallback

### 2026-04-16 — 출처 추적 + 정확도

- **이슈/프로젝트/이니셔티브 제목 작성 지침 강화** (`3f8d977`)
- **assignee 자동 배정 지침 + L'Oreal 프로젝트 세팅** (`d79b6cf`)
- **이모지 이슈 생성 시 슬랙 permalink를 Linear 코멘트로 자동 첨부** (`b13e97b`)
  - 이슈를 보면 원본 Slack 스레드를 한 번에 추적 가능

### 2026-04-17 — 운영 자동화

- **Linear 워크스페이스 자동 세팅 스크립트** (`d5a4591`)
  - `npm run setup:linear` — 멱등 실행, 워크플로우 상태/Estimation/뷰 일괄 생성
- **워크스페이스 레벨 뷰 + 프로젝트 뷰 가이드** (`98b2654`)

### 2026-04-20 — 컨텍스트 보강

- **리액션 이슈 생성 시 스레드 대화 전체를 Linear 코멘트로 포함** (`bdaeed3`)
  - permalink만이 아니라 발화자·시각 포함 마크다운으로 보존

### 2026-04-21 — 운영 가시성

- **데일리 스크럼 스레드 포스트에 인터뷰 시작 시각 표시** (`9f22871`)
  - 누가 언제 응답했는지 한눈에

---

## 버그 픽스

### Slack 이벤트 처리

- **모델명 수정 및 Slack 이벤트 처리 개선** (`fbea628`, 2026-04-13)
- **모델 ID를 `claude-sonnet-4-6`으로 수정** (`e84812d`, 2026-04-13)
  - Anthropic API `-latest` 접미사 미지원 → 정확한 ID 사용
- **`after()`로 Slack 이벤트 백그라운드 처리 보장** (`84ce8d2`, 2026-04-13)
  - Vercel Functions에서 fire-and-forget 패턴이 silently drop되던 문제 해결
- **봇 자신의 메시지 무시** (`76223b9`, 2026-04-13)
  - `cannot_dm_bot` 에러 수정. `bot_id`/`app_id` 기반 필터링 (하드코딩 ID 제거)
- **데일리 스크럼 스레드 ts를 슬랙 채널에서 동적 조회** (`598f1e7`, 2026-04-16)

### AI 응답·이슈 추출

- **AI 응답에서 마크다운 코드펜스 제거 후 JSON 파싱** (`30db9da`, 2026-04-13)
  - 추후 `tool_use` structured output으로 근본 해결
- **이슈 등록 품질 대폭 개선** (`772ac0b`, 2026-04-13)
- **이모지 이슈 등록 품질 대폭 개선** (`a72b462`, 2026-04-13)
- **`:task:` 커스텀 이모지 등록 + reaction 로깅 추가** (`8138958`, 2026-04-13)
- **`emoji-task` 커스텀 이모지명 추가** (`f73071f`, 2026-04-13)
- **이슈 추출 시 팀 분류가 항상 PROJ로 가던 문제 해결** (`222b361`, 2026-04-20)

### Linear 연동

- **Linear 팀 조회를 실제 팀(BUB)으로 수정** (`7f48260`, 2026-04-13)
- **라벨 워크스페이스 충돌 해결 + 이슈 링크 클릭 가능하게 표시** (`f87a4ea`, 2026-04-17)
- **팀 간 라벨 충돌 + 회의/미팅 이슈 제외** (`f145a28`, 2026-04-17)
  - 워크스페이스/팀 라벨 스코프 규칙 정립

### 인터뷰·데일리 스크럼 플로우

- **인터뷰 종료 시 이슈 생성 차단 원인 제거** (`c922fe7`, 2026-04-17)
- **follow-up 1회로 제한, 마무리 인사 후 바로 이슈 생성** (`e8291d5`, 2026-04-17)
- **이슈 생성 예외 케이스 대폭 보강** (`6f2ed37`, 2026-04-17)
- **데일리 스레드 없으면 자동 생성 + 외부 Cron 준비** (`a17d3ba`, 2026-04-17)
- **데일리 스크럼 스레드에 AI 요약 리스트로 포스팅** (`a9a1732`, 2026-04-17)
- **데일리 스레드에 이슈 링크 + 기타 할 일 분리 표시** (`abec78a`, 2026-04-17)
- **스레드에 미응답자 태그하는 기능 제거** (`3c717af`, 2026-04-20)
  - 자동 멘션이 부담을 주는 부작용 → 제거

---

## 회고 메모

- **CEO Review 결과 (HOLD SCOPE)**: 4/13 시점에 범위 확장 대신 기존 기능 안정화 우선 결정. 이후 일주일간 이 기조로 진행.
- **인터랙티브 플로우 제거**: Vercel Fluid Compute 인스턴스 간 메모리 공유 불가로 5분 타임아웃 발생 → `askProjectSelection`/`checkDuplicateAndAsk` 호출 제거하고 `projectId=undefined` fallback으로 단순화.
- **테스트 인프라**: Vitest + 33개 유닛 테스트 (sanitize 16, retry 14, labels 7). `@linear/sdk` 클라이언트는 `vi.mock`으로 격리.
- **저장소 전환 대기**: Upstash Redis 프로비저닝(`KV_*`)은 완료. 대화 저장소(`src/lib/conversation/store.ts`)를 in-memory Map → Redis로 옮길 때 사용 예정.
